import Testing
import Foundation
@testable import HealthRecordingApp

@Suite("Minimal Debug Test")
struct MinimalTest {
    
    @Test("Simple test without HealthRecord creation")
    func testBasicFunction() {
        let result = 2 + 2
        #expect(result == 4)
    }
    
    @Test("Test MockHealthRecord creation - should work without SwiftData")
    func testMockHealthRecordCreation() {
        // Use mock instead of actual SwiftData model
        let record = MockHealthRecord(type: .weight, value: 70.0, unit: "kg", source: .healthKit)
        #expect(record.value == 70.0)
        #expect(record.type == .weight)
        #expect(record.unit == "kg")
    }
}